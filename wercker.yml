box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.20"
        theme: kube
        flags: --buildDrafts=true
deploy:
  setps:
    - lukevivier/gh-pages@0.2.1:
      token: $GIT_TOKEN     
      basedir: docs